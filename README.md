# Hand-ArM2
A 3d model interaction application

To run this application refer to the instructions in SETUP.md. Below is a quick summary:
git clone <your-repo>
cd Hand-AR
python3 -m venv handtrack_env
source handtrack_env/bin/activate
pip install -r requirements.txt
python3 opening.py
